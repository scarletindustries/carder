name = "twocore"
version = "1.0.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
# description = ""
# licences = ["Apache-2.0"]
# repository = { type = "github", user = "", repo = "" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 1.0.0 and < 2.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"
argv = ">= 1.1.0 and < 2.0.0"
simplifile = ">= 2.5.0 and < 3.0.0"
# arc's JS parser is the front end of `twocore/frontend/js` (JS → 2core IR → BEAM).
# Only its parser/AST is used; the lowering + backend are 2core's.
arc = { git = "https://github.com/alii/arc.git", ref = "e1e8868f49ad17deb85a9469caf2bc100adac226" }

[dev_dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
