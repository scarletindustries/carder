# Implementation State — the live ledger

> The swarm's shared ledger for **the phase currently in flight**. Before claiming work, read it;
> after finishing, update it. It is a *working* file, not a history archive — completed phases are
> compacted into [`01-status.md`](01-status.md) once proven, and this file resets to the template
> below for the next phase.
>
> **How to use this file:** [`03-phase-workflow.md`](03-phase-workflow.md) §7.
> **Where we are:** [`01-status.md`](01-status.md) · **What's next:** [`02-roadmap.md`](02-roadmap.md)
> · **Architecture:** [`00-high-level.md`](00-high-level.md)

**Legend — unit status:** `unclaimed` · `in-progress (name)` · `blocked (on …)` · `done`
**Legend — freeze milestone:** a published, compiling type/signature stub that unblocks downstream
units. Announce it here the moment it lands (`FROZEN ✓` / `published ✓`).

---

## Current phase

**Two phases scoped, neither in flight yet — awaiting review; no code.** Phase 11 (`--link`) and Phase 12
(typed bindings) are **independent** (11 = runtime *inclusion* into one `.beam`; 12 = typed host-language
*access* to a `.beam`); either may land first, and they compose (a linked `.beam` + typed bindings).
Recommended order: **Phase 11 first** (it settles the runtime/compiler layer split), then Phase 12.

---

### ▶ Phase 11 — Self-contained output (`--link`) — ✅ **PHASE 11 PROVEN** (all units `done`; capstone P11-06 landed)

Overview: [`phase-11/00-overview.md`](phase-11/00-overview.md). **AUTHORITATIVE:**
[`phase-11/RECONCILIATION.md`](phase-11/RECONCILIATION.md) (decisions `R1–R17`; overrides the overview on
conflict). Goal: an optional `--link` flag on `to-beam-wasm` that merges the runtime dependency closure
into the generated module (whole-program Core-Erlang merge via `cerl` FFI + DCE), producing a **single
self-contained `.beam`** that runs on a bare Erlang/OTP node. Default output stays byte-identical.
Decisions `O1–O8`; units `P11-01 … P11-06`.

The fan-out + 5-lens adversarial critique **executed the merge on OTP 29** (feasible, GO) and corrected
the linker algorithm — chiefly that `fun M:F/A` function-value captures (332 remote + 43 local in the
closure) are first-class for both reachability and rewrite (R4/R5), the `instantiate/N` seed is a DCE
root (R6), the manifest must include `gleam_stdlib.erl` (R8), the allowlist is mechanically derived
(R7), and the D3a check is structural over `cerl` not a text grep (R9).

Phases 1–10 are complete and proven on `main` — see [`01-status.md`](01-status.md) §3.

**Baseline entering Phase 11:** 1827 gleam tests / 0 fail · `gleam build` zero warnings · `gleam format`
clean · WASM conformance **46,529 pass / 1,768 skip / 0 fail** (Safe ≡ Unsafe, every
`state_strategy × mem_tier`) · JS-on-BEAM 52 / 0 / 3.

### Freeze milestones

| Milestone | Produced by | Status | Unblocks |
|---|---|---|---|
| `«RT-LAYER-FROZEN»` — runtime reaches zero compiler modules (`OptLevel` relocated to the leaf `twocore/opt_level`; `ir` NOT split) | P11-01 | `FROZEN ✓` | P11-02 |
| `«CLOSURE-FROZEN»` — link-closure manifest (`src/twocore/backend/link_manifest.gleam`) + acquisition method + mechanically-derived OTP-ambient allowlist + mangle/mergeability invariants + drift test | P11-02 | `FROZEN ✓` | P11-03 |
| `«LINKER-IFACE-FROZEN»` — `beam_link.link_program`/`link_to_core` public signatures + `LinkError` variants | P11-03 | `FROZEN ✓` | P11-04, P11-06 |
| `«BARE-NODE-HARNESS-PROVEN»` — `test/twocore_linked_boot_ffi.erl` scrubbed fresh-`erl` boot harness (`boot_invoke/5` + `compile_source/2` + `which_in_parent/1`; exit/stdout contract `0`+`RESULT:`/`0`+`TRAP:`/`3`+`LEAK:`/`4`+`NOLOAD:`/`127`) with an in-child `code:which` isolation gate; proven BOTH directions (self-contained `.beam` → `RESULT:42` exit 0; a `twocore@` module on the child path → `LEAK:` exit 3, no `RESULT:`) | P11-05 | `FROZEN ✓` | P11-06 (L2) |

### Units

| Unit | Owner / status | Depends on (freeze) | Leaves |
|---|---|---|---|
| **P11-01** Keystone: runtime layer split (`OptLevel`→leaf; `ir` not split, R2/R3) | `done` | — | Runtime reaches zero compiler modules (grep-verified); default output byte-identical; the ~10-file reach set repointed. |
| **P11-02** Link-closure manifest + allowlist + acquisition + invariants (R1/R7/R8/R12/R15) | `done` | `«RT-LAYER-FROZEN»` | Frozen closure set (16 runtime + 12 gleam + 7 FFI, incl. `gleam_stdlib`+`gleam_erlang_ffi`+5 `twocore_rt_*_ffi`), per-module Core-acquisition method, mechanically-derived allowlist (15), `__`-free + mergeability invariants + genuinely-recomputing drift test. |
| **P11-03** The `cerl` linker engine (R4/R5/R6/R9/R10/R11) | `done` | `«CLOSURE-FROZEN»` | `beam_link.link_program`/`link_to_core` + `twocore_linker_ffi.erl`: 3-node-class rewrite incl. fun-captures, funref+`instantiate` reachability roots, DCE, deterministic `from_core`, built-in fail-closed structural D3a check; in-process smoke differential green (built+loaded+called, bit/trap-identical). |
| **P11-04** CLI `--link` (`to-beam-wasm` only) + `build_beam` entry + fail-closed gate (R13/R14) | `done` | `«CLOSURE-FROZEN»` (ambient allowlist), `«LINKER-IFACE-FROZEN»` (sig) | `--link` default off (byte-identical); tier-N + import-bearing + `on_load` rejected at the CLI/linker boundary. |
| **P11-05** Bare-node isolation harness, proven first (test-first) | `done` | — | `test/twocore_linked_boot_ffi.erl` (`boot_invoke/5`, `compile_source/2`, `which_in_parent/1`) + `test/twocore/backend/linked_boot_test.gleam` (6 tests): scrubbed fresh `erl` (drops `ERL_LIBS`/`ERL_*FLAGS`, `-boot no_dot_erlang`, isolated `-pa`), in-child `code:which` isolation gate over the representative closure set (halts nonzero on any hit), seed-then-call (R6) in one process with cell/threaded self-detection. Self-test proves the gate GATES (twocore@ on path → exit 3 `LEAK:`, no `RESULT:`) and reports success on a hand-authored self-contained `.beam` (exit 0 `RESULT:42`). |
| **P11-06** Capstone | `done` — **PHASE 11 PROVEN** | all above | **PHASE 11 PROVEN.** L1 in-process linked≡non-linked differential (full corpus × mode × state × tier P/O) + L2 bare-node differential (import-free subset) + constant-space + determinism byte-check + D3a corpus assertion; `docs/phase-11-linking.md`; `01-status.md` §5. |

### Landing log

- **P11-06 — CAPSTONE — PHASE 11 PROVEN** — 1922 tests (was 1909, +13 `linked_selfcontained_test`), conformance unchanged (46,529/1,768/0), **0 failures**, 0 new warnings (the 2 pre-existing `rt_js_test` remain), format clean, byte-identical default output (the capstone only drives frozen public APIs + edits status/docs — touches no `src/` file). ~99s total suite (baseline ~78s + ~20s for the capstone's ~140 in-process merges + 14 real `erl` child boots). The differentials genuinely EXECUTE (built + loaded + called / booted), so the verdict is proof, not assertion.
  - **L1 (in-process linked ≡ non-linked, O5).** The tier-sensitive core corpus (`combos.corpus_programs`, 14 programs) × the FULL 8-way `{Safe,Unsafe} × {Cell,Threaded} × {Paged,Atomics}` matrix (tier-N excluded, O8): every linked artifact (via `beam_link.link_program`, loaded in-process) returns **bit-pattern-identical, trap-identical** `Outcome`s to the non-linked oracle (`driver.pipeline_with`) AND is spec-correct vs `corpus/*.expected` (`combos.evaluate` + `combos.identity_across`). The reference/SIMD/bulk/EH extended corpus (12 programs — `reftab`/`simd*`/`bulkmem`/`mem64`/`multimem`/`eh*`) × `{Unsafe cell·paged, Unsafe threaded·paged}` proves the R4 capture surface (`rt_simd`'s remote `fun` captures) + the `rt_exn` closure merge for BOTH strategies. Focused regressions: **R4** (fun-captures first-class — `simddot` + a Safe-metered build whose metering closure captures `gleam@dynamic@decode:decode_int/1`), **R5** (intra-module apply links — `fib` under all 8 bindings returns `Ok`), **R6** (`instantiate/N` DCE root — `mem`/`gvar` seed correctly), and an **authored import-bearing fixture** (imported `spectest.global_i32 = 666`) whose merged `instantiate/1` seeds + reads correctly in-process (honest-scope home, omitted from L2 per R14).
  - **L2 (bare-node differential, measured).** The import-free subset (`sum_to`/`mem`/`intops`/`simddot`/`reftab`, all i32-in/i32-out or trap) × strategy × `{tier-P, tier-O}` (12 points): `boot_invoke` boots the merged `.beam` on a scrubbed fresh `erl` (only that `.beam` on `-pa`), and the child's `RESULT:`/`TRAP:` matches the in-process oracle (`pipeline.run_source`) bit/trap-identically — covering all four cell/threaded × paged/atomics corners + a value AND a trap point (OOB memory, div-by-zero, uninitialized-element). Because the in-child `code:which` gate halts BEFORE the invoke on any `twocore@`/`gleam@` hit, a `RESULT:` at exit 0 **PROVES isolation held** (no `twocore@`/`gleam@` reachable) — the differential IS the isolation proof. **Constant-space:** `sum_to(100000)` boots on the isolated child and COMPLETES at exit 0 with the exact i32-wrapped value (a per-iteration leak would exhaust the child's default heap and not complete), AND the linked artifact's own runtime holds the loop in constant space in-process (`ffi.gc_and_memory`, `mem_big < mem_small*4`).
  - **Hygiene.** **Determinism (R10):** `link_program` twice on `mem` ⇒ byte-identical `.beam`. **D3a (R9):** an independent STRUCTURAL assertion over the merged Core (`link_to_core`) of the whole import-free corpus — every remote `call 'MOD':…` targets an ambient-allowlist module, no `call 'erlang':'apply'`, no residual off-closure `fun 'twocore@…'`/`fun 'gleam@…'` capture; sound against false-positives (legitimate first-class `apply Op(…)`/`call_indirect` render as `apply …`, never `call '`/`fun '`), complementing the linker's own fail-closed self-check (whose passing is proven by `link_to_core` returning `Ok` over the corpus). **Exports (R11):** the merged header exports EXACTLY the thin generated header's set (public exports + `instantiate/N`) ∪ `{module_info/0, module_info/1}` — no leaked per-module `module_info`, no exported runtime helper. **Fail-closed confirm (R13/R14):** `twocore.link_gate` refuses tier-N (`LinkTierNif`) and import-bearing (`LinkImportBearing`) and admits import-free tier-P.
  - **⚠ Signal handled (NOT a linker bug):** `mem64` under a **Safe (`MeterFuel`)** binding traps `{wasm_trap,fuel_exhausted}` (its large-memory grow exceeds the default fuel budget — a fuel-budget property of the Safe posture, IDENTICAL in linked AND oracle, so the merge is correct and identity holds). The extended corpus is therefore driven under **Unsafe** (`MeterOff`) bindings where the oracle is spec-correct over the whole set; the Safe/metered posture is fully covered on the tier-sensitive CORE corpus (8-way) and the metering fun-capture regression. This is a test-binding choice, not a defect. **No other divergence found** — linked ≡ non-linked everywhere.
  - **Files:** created `test/twocore/backend/linked_selfcontained_test.gleam` (13 tests) + `docs/phase-11-linking.md`; edited `specs/01-status.md` (§1 summary + metric 1922, §3 Phase-11 ledger row, §5 the `--link` self-contained path + R17 disambiguation) + `specs/state.md` (this log). Touches NO `src/` file (drives frozen public APIs only). The linked driver ports `driver.gleam`'s private `instantiate_typed` chain, swapping ONLY the compile tail (`compile_and_load` → `link_program`+`load_module`) — self-checking, since L1's linked≡oracle identity would go red on any drift.
- **P11-05** — 1909 tests (was 1903, +6 `linked_boot_test`), conformance unchanged (46,529/1,768/0), 0 new warnings (the 2 pre-existing `rt_js_test` remain), format clean, byte-identical default output (test-only infra; imported by nothing on the pipeline). `«BARE-NODE-HARNESS-PROVEN» ✓`. The self-test genuinely spawns real fresh `erl` children (5 child spawns across the suite) and MEASURES isolation.
  - **Frozen harness** — `test/twocore_linked_boot_ffi.erl` (test-only trust boundary: only `os`/`file`/`compile`/`code`/`erlang`/`open_port`; NOT the OTP-internals boundary of `twocore_linker_ffi`). API P11-06 · L2 consumes: `boot_invoke(Beam, ModuleName, Fun, Args, Extra) -> {ExitStatus, CombinedStdoutStderr}` — boots `Beam` on a scrubbed/isolated fresh `erl` and runs `ModuleName:instantiate()` then `Fun(Args)` in ONE child process (R6 seed-then-call, cell/threaded self-detected). `compile_source(ModuleName, Source) -> {ok, Beam}|{error, Text}` (manufacture fixtures w/o the linker). `which_in_parent(Mod) -> {ok, PathBin}|{error, nil}` (anti-vacuity: prove gate atoms real).
  - **Frozen exit/stdout contract** (Gleam side `#(Int, String)`): `0`+`RESULT:<~0p pkg>` = ran clean, isolation held (the gate halts BEFORE invoke on any hit, so a `RESULT:` at 0 PROVES isolation); `0`+`TRAP:<~0p reason>` = export trapped (a trap ≠ a leak); `3`+`LEAK:<mod>` = isolation gate hit; `4`+`NOLOAD:<mod>` = module-under-test not on the child path; `124` = defensive child-timeout (CI safety, 60s bound); `127` = no `erl`. A TRAP is signalled to the parent as exit 0 with a `TRAP:` line carrying the `~0p`-rendered reason (substring-matchable, e.g. `wasm_trap`).
  - **Isolation is IMPLEMENTED then MEASURED:** env scrub via the port `{env, [{"ERL_LIBS",false},{"ERL_FLAGS",false},{"ERL_AFLAGS",false},{"ERL_ZFLAGS",false}]}` (drops the only env-based path-injection vectors) + `-boot no_dot_erlang` + an isolated `-pa LinkDir` holding only the module under test (Extra beams go on a SEPARATE `-pa`); then the in-child runner probes `code:which/1` over `['twocore@runtime@rt_mem','gleam@list',gleam_stdlib,'twocore@ir']` and halts 3 on ANY hit. The NEGATIVE self-test (`gate_fires_when_twocore_module_on_child_path_test`) injects a compiled `twocore@runtime@rt_mem` stub on the child `-pa` → child halts exit 3 `LEAK:twocore@runtime@rt_mem`, no `RESULT:` — the gate genuinely gates (non-vacuous). The POSITIVE (`reports_result_on_trivial_selfcontained_beam_test`) boots a hand-authored self-contained `.beam` (calls only `erlang:error`/`+`) → exit 0 `RESULT:42`. `gate_atoms_are_real_test` proves all four gate atoms resolve in the parent (so the child's `non_existing` is a real absence).
  - **⚠ DISCOVERED-WRONG — signal for P11-06:** (1) `05-bare-node-harness.md` §3 step 2 said write `Beam` to `<ModuleName>.beam`, which makes `code:which(ModuleName)` ALWAYS find a file by that name ⇒ the NOLOAD guard (and test 6's name-mismatch premise) is UNREACHABLE (a mismatch yields a load-mismatch trap/exit 0, not `non_existing`/exit 4). FIX: `boot_invoke` writes the beam under its OWN declared module name (via `beam_lib:info`), which is IDENTICAL for P11-06 (link_program guarantees the returned atom == the beam's declared module) but makes NOLOAD meaningful — a `ModuleName` disagreeing with the beam now yields exit 4. (2) §4 sketched `which_in_parent` returning a bare `error` atom; the frozen Gleam binding is `Result(String, Nil)`, whose `Error(Nil)` wire shape is `{error, nil}` (repo convention, e.g. `twocore_rt_state_ffi`) — implemented as `{error, nil}`.
  - **Files:** created `test/twocore_linked_boot_ffi.erl` + `test/twocore/backend/linked_boot_test.gleam`. Touches no unit-owned source and no pipeline/registration point (purely additive test infra).
- **P11-03 (reopened → fixed)** — the R4 edge-(a) fun-capture-target survival bug flagged by P11-04 (line below) is FIXED. New Safe-mode linked regression `beam_link_test.safe_metered_fun_capture_target_survives_dce_test`: a metering-on (`MeterFuel`) tier-P import-free module lowered through the REAL `pipeline.ir_to_core`/`ir_lower` path (so the `rt_meter` fuel closure with the `gleam@dynamic@decode:decode_int/1` capture is emitted) links, loads, and calls with no `undef` (failed first with the exact `…decode__run/2 → decode__decode_int/1` undef, then passed). **Root cause was actually TWO bugs** (the reachability walk was NOT the (only) culprit — decode_int WAS being pulled into the merged Core text): (1) the capture rewrite emitted `erlang:make_fun('Merged','M__F',A)`, which is OPAQUE to the Erlang compiler's OWN call-graph, so `compile:forms` DCE'd the non-exported target back OUT of the loaded `.beam` → runtime `undef` — fixed by emitting a bare LOCAL funref `'M__F'/A` (`c_fname` value = a real call-graph edge the compiler keeps; verified A/B/C). (2) captures NESTED in a compound literal (gleam's `Decoder` = a single `#c_literal{val={decoder, fun …/1}}`) were missed by BOTH the reachability walk AND the rewrite (a latent bare-node hole) — fixed by scanning `term_ext_funs/1` for external funs embedded ANYWHERE in a term (tuple/list/map) and rebuilding the literal as a constructor expression (`term_to_expr/3`) with each embedded capture replaced (in-closure → local funref; ambient → `erlang:make_fun`). The D3a literal check now scans embedded (bare+nested) funs and fails closed on any residual off-closure capture. **No manifest change needed** (`gleam@dynamic@decode` already in `frozen_gleam_closure`). Verified on the merged Safe metered core: 0 residual external `fun 'gleam@…'` captures, `{'decoder','gleam@dynamic@decode__decode_int'/1}` rebuilt with a local funref, `dce_only_remotes` (code/net_kernel/timer) still absent, existing smoke/rt_simd-capture/D3a tests still green. **1903 passed, 0 failures** (was 1902 after P11-04, +1). Format/build clean (only the 2 pre-existing `rt_js_test` warnings). Files touched: `src/twocore_linker_ffi.erl` (rewrite/reachability/D3a), `test/twocore/backend/beam_link_test.gleam` (new regression). This unblocks P11-06's Safe-mode corpus differential — P11-04 may now switch its positive smoke back from `--unsafe` to Safe if desired.
- **P11-04** — 1902 tests (was 1896, +6: 3 `link_gate_test` + 3 `cli_link_flag_test`), conformance unchanged (46,529/1,768/0), 0 new warnings (the 2 pre-existing `rt_js_test` remain), format clean, **default output byte-identical** (proven by `cli_link_flag_test.default_off_byte_identical_test`: the `--link`-absent CLI `.beam` bytes equal a fresh `pipeline.core_to_beam` over the identical `resolve_binding` binding). No new freeze (leaf wiring unit).
  - **CLI surface added:** a `--link: Bool` axis flag (default `False`) on the `to-beam-wasm` verb ONLY (R13). Added to the `Axes` record + parsed in `do_split_axis_flags`; threaded through `with_binding` (whose continuation now takes `(binding, link, positionals)`). Scoping is fail-closed: `emit`/`to-core`/`run` reject `--link` via `reject_link/3` (short-circuits before file IO) and `to-beam`/`build` reject a `--link` token explicitly (R13 deferral — no `Binding` to gate). New `build_beam` linked entry: `build_beam.link_beam(generated_core: BitArray, module_name: String) -> Result(#(Atom, BitArray), beam_link.LinkError)` — composes `link_manifest.ambient_allowlist()` with `beam_link.link_program/3` (single point binding the OTP-ambient set to a link; CLI never re-spells the allowlist).
  - **Three fail-closed rejections** (each a non-zero-exit CLI `Error`): (1) **tier-N** — `link_gate(binding, m) == Error(LinkTierNif)` iff `binding.mem_tier == Nif`, under ANY mode (Unsafe+Nif too); message "`--link cannot merge the nif memory tier … tier-P/O only`". Pinned distinct from `profiles.link/1` by asserting `profiles.link` returns `Ok` for the SAME Unsafe+Nif binding (R13). (2) **import-bearing** — `Error(LinkImportBearing(n))` iff `m.imports != []` (conservative superset incl. import-but-uncalled); message "`--link cannot link an import-bearing module (n import(s)) …`". (3) **`on_load`/unmergeable INSIDE the closure** is NOT the CLI gate's job (R13) — the linker discovers it structurally as `UnmergeableConstruct`; `describe_beam_link_error/1` renders all 7 `beam_link.LinkError` variants to stderr on the linked branch.
  - **Positive `--link` path proven:** `to-beam-wasm --unsafe --link add.wasm out.beam` writes ONE self-contained `.beam`; `exec` (load → spawn → `instantiate/0` → invoke) returns `add(2,3)==5`, equal to the non-linked build (`cli_link_flag_test.linked_build_smoke_test`).
  - **⚠ DISCOVERED-WRONG — signal for P11-06/P11-03 (a real P11-03 linker gap, NOT a P11-04 defect):** the default **Safe** pipeline's fuel metering (`ir_lower` → `charge` → `rt_meter`) reaches `gleam@dynamic@decode:decode_int/1` via a **fun-capture**; the linker REWRITES the capture to the local mangled call `gleam@dynamic@decode__decode_int/1` but does NOT pull that capture's target **DEF** into the merge, so a linked Safe `add` traps `undef` at runtime on the first `charge` (stacktrace: `…decode__run/2 → decode__decode_int/1` undefined). P11-03's `beam_link_test` never caught this because its `gen_core` uses `emit_core.emit_module` DIRECTLY (no `ir_lower`, so no `charge`/metering closure). The positive smoke therefore uses `--unsafe` (`MeterOff` ⇒ no `charge` ⇒ the metering closure is never emitted) — still a genuine tier-P (`Paged`), import-free linked build. **P11-06's Safe-mode corpus differential will fail until P11-03's reachability walk also seeds fun-capture *targets* as DCE roots (R4 edge (a)), not only rewrites them (R4/R5 edge (b)).**
  - **Files:** edited `src/twocore.gleam` (flag/scoping/gate/linked branch/usage) + `src/twocore/backend/build_beam.gleam` (`link_beam/2`); created `test/twocore/link_gate_test.gleam` + `test/twocore/cli_link_flag_test.gleam`. No default-path behavior changed.
- **P11-03** — 1896 tests (was 1881, +15 `beam_link_test`), conformance unchanged (46,529/1,768/0), 0 new warnings (the 2 pre-existing `rt_js_test` remain), format clean, byte-identical default output (the linker is a new emission path invoked by nothing yet). `«LINKER-IFACE-FROZEN» ✓`. The in-process smoke differential genuinely EXECUTED the merge: emit numerics IR → `.core` → `link_program` → load → call; linked ≡ in-process, value+trap identical (`i32.clz(1)=31`, `i32.clz(0)=32`, `i32.add(0x7FFFFFFF,1)=0x80000000`). Standalone de-risk also proved the critique's cross-module chain (`chain(100,6)=106`).
  - **FROZEN public interface** (`src/twocore/backend/beam_link.gleam`, P11-04/P11-06 build against this): `link_program(generated_core: BitArray, module_name: String, ambient: List(String)) -> Result(#(Atom, BitArray), LinkError)` and `link_to_core(generated_core: BitArray, module_name: String, ambient: List(String)) -> Result(#(Atom, String), LinkError)` (merged Core TEXT before compile — P11-06's independent-D3a seam). Guarantee: the returned `Atom` equals the module name declared inside the returned `.beam` (P11-05 resolves the child via `code:which`). `LinkError` variants (7): `OffAllowlistRemote(module, fun)`, `MissingClosureModule(module)`, `AmbientAuthorityFound(detail)`, `UnmergeableConstruct(detail)`, `MangleCollision(a, b)`, `MalformedCore(detail)`, `CoreAcquisitionFailed(module, reason)`.
  - **The `cerl` merge** (`src/twocore_linker_ffi.erl`, same pinned-OTP-29 trust boundary as `twocore_codegen_ffi`): acquire generated Core from TEXT (`core_scan`/`core_parse`) + discovered members from RESIDENT `.beam` `debug_info(core_v1)` (verified: `beam_lib:chunks` → `Backend:debug_info(core_v1,…)`); reachability worklist from the generated exports + `instantiate/N`, following 3 edge kinds (remote `#c_call`, intra-module `apply` on an `fname`, fun-capture literal); function-level DCE (only reached defs assembled); mangle every DISCOVERED module's def to `'M__F'/A` (the generated module is the IDENTITY so its public exports stay callable); rewrite 3 node classes via `cerl_trees:map`; synthesize one `module_info/{0,1}` + drop all source attrs; strip node annotations + sorted def order (R10) → `compile:forms([from_core,binary,deterministic])`; built-in fail-closed structural D3a check refuses to emit on `erlang:apply`/computed-module call/off-allowlist remote/residual off-closure capture.
  - **Load-bearing discovered facts for P11-04/P11-06:** (1) **Fun-captures (R4) reconstruct as a `literal` node wrapping an EXTERNAL `fun` VALUE** (not a `make_fun` call, not a distinct AST kind) — detected via `erlang:fun_info(V,type)==external`; `rt_simd:f32x4_add/2` captures `fun twocore@runtime@rt_num:f32_add/2` (the exact R4 example, tested). In THIS build ALL captures are external (Gleam fully-qualifies) — no `local`-type fun literals were found. (2) **`cerl_prettypr:format` CRASHES on an external-fun literal** (`case_clause`); the linker sidesteps this by rewriting every capture to an explicit `erlang:make_fun('MergedMod','M__F',A)` call (printable via `core_pp`, compilable, and D3a-safe: literal self-module + literal local name, explicitly NOT `erlang:apply`) and prints merged Core via `core_pp:format`. (3) **`code`/`net_kernel`/`timer` (`dce_only_remotes`) do NOT survive** — even though the numerics `instantiate` pulls `gleam_erlang_ffi`, function-level DCE never reaches its unused `connect_node`/`priv_directory`/`sleep` helpers (asserted). (4) **Design note**: the merged module's public exports are the generated module's ORIGINAL export names UNMANGLED (the generated module is mangle-identity); only DISCOVERED closure modules are `'M__F'`-mangled — so `core_lint`/`from_core` accept the export list unchanged and no re-export wrappers are synthesized. (5) **`OffAllowlistRemote` is a D3a backstop**: in practice a reached non-ambient module is acquired+merged (its remotes become local applies) or fails as `MissingClosureModule`/`CoreAcquisitionFailed`; a real non-ambient OTP module (e.g. `filelib`) fails closed on acquisition (no `core_v1` chunk, no source on the path).
  - **Deliberate cross-file reaches (P11-03-owned):** creates `src/twocore/backend/beam_link.gleam`, `src/twocore_linker_ffi.erl`, `test/twocore/backend/beam_link_test.gleam`, and the read-only synthetic-module installer `test/twocore_linker_test_ffi.erl` (test-dir, mirrors the `test/twocore_*_test_ffi.erl` convention — installs a `__`-bearing and an `-on_load` module on disk so the real acquisition path exercises `MangleCollision`/`UnmergeableConstruct`). Reuses `build_beam`'s `compile_and_load`/`load_module` and `twocore_emit_test_ffi:catch_apply` read-only. Touches no default-path source.
- **P11-02** — 1881 tests (was 1865, +16: 11 `link_manifest_test` + 5 `link_manifest_drift_test`), conformance unchanged, 0 new warnings, format clean, byte-identical default output (manifest is imported by nothing yet). `«CLOSURE-FROZEN» ✓`.
  - **Frozen manifest** — `src/twocore/backend/link_manifest.gleam` (stdlib-only leaf: `gleam/list`+`gleam/set`+`gleam/string`, zero project imports). Public API P11-03 consumes: `ambient_allowlist/0`+`is_ambient/1` (the 15-module DCE stop-set + fail-closed remote check), `Acquisition`+`primary_acquisition/1`+`fallback_acquisition/0` (R1 order: `GeneratedCoreText` for the wasm module, uniform `ResidentBeamCore` for discovered members, `CompileFileToCore` fallback), `mangle_separator`+`mangle_injective/1` (R12 precondition), and the `frozen_*` snapshots.
  - **Enumerated closure (measured mechanically from the shipped `.beam`s, module-level `beam_lib:imports` walk from `frozen_runtime_roots/0` — R4-complete: the `imports` chunk records `fun M:F/A` captures too):** 16 runtime (`link`,`porffor_abi`,`rt_exn`,`rt_host`,`rt_mem`,`rt_mem_atomics`,`rt_meter`,`rt_num`,`rt_ref`,`rt_simd`,`rt_state`,`rt_stdlib`,`rt_table`,`rt_table_atomics`,`rt_table_ets`,`rt_trap`) + 12 gleam (`bit_array`,`dict`,`dynamic`,`dynamic@decode`,`erlang@atom`,`float`,`int`,`list`,`result`,`set`,`string`,`string_tree`) + **7 FFI** (`gleam_stdlib`, `gleam_erlang_ffi`, + the 5 `twocore_rt_{exn,mem_atomics,ref,state,table_ets}_ffi`) = 35 mergeable modules. Allowlist = 15 (`erlang,lists,maps,binary,math,ets,atomics,unicode,string,io,io_lib,io_lib_format,base64,rand,uri_string`); `frozen_surviving_remotes/0` == the allowlist exactly. `__`-free ✓; no `-on_load`/behaviour/`persistent_term`/NIF/`gleam@@main` across the closure ✓.
  - **Spec-drift notes for downstream units (P11-03/P11-06):** (1) **The FFI bucket is 7, not the spec's 6** — the real closure includes **`gleam_erlang_ffi`** (reached `rt_host` → `gleam@erlang@atom:decoder/0` → `gleam_erlang_ffi:atom_from_string/identity`); the spec §3 `frozen_ffi_erl` list omitted it. (2) **A module-level walk over-approximates surviving remotes to 18** — merging `gleam_erlang_ffi` wholesale pulls `code`/`net_kernel`/`timer` via its *unused* `connect_node`/`priv_directory`/`sleep` helpers; these are eliminated by the linker's FUNCTION-level DCE (never reached from the atom path), so they are frozen as `dce_only_remotes/0` and kept OUT of `ambient_allowlist/0` (no D3a-weakening `code` widening). `frozen_surviving_remotes/0` (15) == module-level-ceiling (18) − `dce_only_remotes/0` (3), and equals R7's measured floor exactly — validating R7. (3) **`rt_js`/`twocore_rt_js_ffi` are correctly EXCLUDED** — only reachable via `CallHost("js",…)`, off the WASM `--link` path (and import-bearing per R14). (4) **`rt_bif` is EXCLUDED** — build-time gate consulted by `ir_lower`, not a runtime call target; `instance`/`profiles`/`rt_mem_nif` also correctly absent. The spec/overview's "runtime/rt_* + instance/profiles" phrasing is looser than the reachability truth.
  - **Deliberate cross-file reaches (P11-02-owned):** creates `src/twocore/backend/link_manifest.gleam`, `test/twocore/backend/link_manifest_test.gleam`, `test/twocore/backend/link_manifest_drift_test.gleam`, and the read-only introspection FFI `test/twocore_link_manifest_drift_ffi.erl` (test-dir, mirrors the existing `test/twocore_rt_*_test_ffi.erl` convention). Touches nothing else. Drift test proven non-vacuous (perturbing a frozen list makes `closure_drift_test` fail — it recomputes from the live build, not the frozen data).
- **P11-01** — 1865 tests (was 1863, +2 `link_layer_freeze_test`), conformance unchanged, 0 new warnings, format clean, byte-identical default output. `OptLevel { OptNone Baseline Aggressive }` relocated to the dependency-free leaf **`twocore/opt_level`** (type now at `twocore/opt_level.OptLevel`); `ir` NOT split (R2). `«RT-LAYER-FROZEN» ✓`.
  - **Deliberate cross-file reaches (P11-01-owned):** `src/twocore/middle/ir_opt.gleam` (deleted the `OptLevel` type block, now imports it from `twocore/opt_level`), `src/twocore/runtime/instance.gleam:74` + `src/twocore/runtime/profiles.gleam:53` (repointed imports — removes the runtime's last compiler-module import), and the 7 test files that name the constructors (`opt_iface_freeze_test`, `profiles_test`, `differential_test`, `phase10_capstone_test`, `baseline_test`, `memory_differential_test`, `aggressive_test`).
  - **Spec-drift note for downstream units:** the unit doc's per-file table for `phase10_capstone_test` omitted a `ir_opt.OptLevel` **type** reference (line 259, `level: ir_opt.OptLevel`); its `opt_level` import needed `type OptLevel` in addition to the `Baseline`/`OptNone` constructors. All other files matched the doc.

---

### ▶ Phase 12 — Typed host-language bindings (Erlang / Elixir / Gleam) — *scoped + critiqued + reconciled*

Overview: [`phase-12/00-overview.md`](phase-12/00-overview.md). **AUTHORITATIVE:**
[`phase-12/RECONCILIATION.md`](phase-12/RECONCILIATION.md) (decisions `R1–R25`; overrides the overview on
conflict). Goal: emit companion typed source files (`.gleam`/`.erl`/`.ex`) giving a native-typed API to
instantiate a compiled module and call its exports — hiding the raw-bit-pattern run-ABI behind
`Int`/`Float`/`BitArray`, traps as `Result`. The `.beam` is unchanged; a **self-contained** droppable
artifact requires Phase-11 `--link` (R21). Decisions `P1–P8`; units `P12-01 … P12-06`. **Threaded
(tier-P, Paged) + export-only this phase** (Cell, import-bearing, mutable tiers deferred).

The fan-out + 5-lens critique **compiled + called all three languages on OTP 29** (GO, no showstopper) and
added R14–R25 — must-fix before freeze: `describe` on the **lowered** module (R17); non-finite floats are a
**sum type** `Finite|NonFinite` (plain `Float` raises on NaN/Inf — R18); the **Stateless/Threaded
two-shape API** resolving the arity-vs-Instance tension (R19); module atom is `twocore@wasm@<base>` not the
file stem (R14); host-name sanitization + cross-language binding-atom collision (R15/R16); reject mutable
mem/table tiers under `--bindings` (R20). Confirmed by experiment: R4 (tuple), R11 (pure value threading,
no process).

### Freeze milestone

| Milestone | Produced by | Status | Unblocks |
|---|---|---|---|
| `«IFACE-DESC-FROZEN»` — `Iface`/`ExportSig`/`StateModel`/`GeneratedFile`/`IfaceError` + `describe/2` (on the lowered module, transitive state-reaching) + the uniform `emit_<lang>(Iface) -> List(GeneratedFile)` signature | P12-01 | `FROZEN ✓` | P12-02, P12-03, P12-04, P12-05, P12-06 |

### Units

| Unit | Owner / status | Depends on (freeze) | Leaves |
|---|---|---|---|
| **P12-01** Keystone: the `Iface` descriptor + value-ABI mapping (R1–R4, R9, R13) | `done` | — | `backend/iface.gleam`; `describe/2` fail-closed on Cell/import-bearing/mutable-tier; `ExportSig.emitted_arity` mirrors `emit_core` exactly; default output unchanged. |
| **P12-02** Gleam emitter — the headline (R5–R9) | `done` | `«IFACE-DESC-FROZEN»` | `emit_gleam_bindings.gleam`: a typed `.gleam` + a tiny `.erl` catch shim (Gleam can't catch in-language) + a usage README; prelude-only so it builds in the dependency-free harness. |
| **P12-03** Erlang emitter (R4–R9) | `done` | `«IFACE-DESC-FROZEN»` | `emit_erlang_bindings.gleam`: one `-spec`'d `.erl`, traps caught in-language (no shim). |
| **P12-04** Elixir emitter (R4–R9) | `done` | `«IFACE-DESC-FROZEN»` | `emit_elixir_bindings.gleam`: one `@spec`'d `.ex`, Erlang-style `try/catch` (no shim, no `rescue` — R23), zero `Elixir.*` runtime deps; compile+call gated skip-if-`elixirc`-absent. |
| **P12-05** CLI `--bindings`/`--out` + folder driver (R8, R12) | `unclaimed` | `«IFACE-DESC-FROZEN»` | `--bindings <langs> --out <dir>` on `to-beam-wasm` (requires `--threaded`); default off ⇒ byte-identical; lowers once, hands one module to `emit_core`+`describe`. |
| **P12-06** Capstone (R6, R10, R11) | `unclaimed` | all above | **PHASE 12 PROVEN.** Per-language compile+call differential (real toolchains) across the type matrix + threaded state; term-ABI oracle for non-Int results; Elixir best-effort skip; `docs/phase-12-bindings.md`; `01-status.md` §5. |

### Landing log

- **P12-04 — Elixir emitter** — 1960 tests (was 1954, +6 `emit_elixir_bindings_test`), conformance unchanged (46,529/1,768/0), **0 failures**, 0 new warnings (the 2 pre-existing `rt_js_test` remain), `gleam format --check src test` clean, **default emission byte-identical** (a new emitter module + tests; the pipeline is untouched).
  - **`emit_elixir(Iface) -> List(GeneratedFile)`** (`src/twocore/backend/emit_elixir_bindings.gleam`) returns exactly **ONE** file — `<base>_bindings.ex` → `defmodule <Camelize(base)>Bindings` (e.g. `twocore_wasm_estate_bindings.ex` → `defmodule TwocoreWasmEstateBindings`) — because Elixir catches the BEAM exception IN-LANGUAGE, so NO companion catch shim. `<base> = iface.sanitize_identifier(module_name)`. The Elixir module compiles to the DISTINCT atom `Elixir.<Camelize>Bindings`, so — unlike the Gleam/Erlang siblings' shared `twocore_wasm_<x>_bindings` — **R16 (cross-language binding-atom collision) does not apply to the Elixir arm** (a different atom namespace); the capstone need not purge the Elixir binding against the other two.
  - **Compile+call PROVEN LOCALLY (not golden), SKIP-if-absent on CI.** The headline test builds two real modules (`describe` Threaded/Paged), `emit_elixir`s them, compiles the emitted `.ex` with the **real `elixirc`** (`twocore_bindings_ffi:compile_load_elixir`), and calls typed exports via `call/3` — each matching the WASM-spec value AND the raw in-process `.beam` oracle (`twocore_threaded_test_ffi`). Matched (elixirc present locally): signed int `add(2,-3)={:ok,-1}` (raw ABI `4294967295`), `add(0x7FFFFFFF,1)={:ok,-2147483648}`; multi-value `divmod(17,5)={:ok,{3,2}}`, signed `divmod(-7,2)={:ok,{-3,-1}}`; trap `divmod(10,0)={:error,{:wasm_trap,:int_div_by_zero}}` (kind atom matched STRUCTURALLY); finite float `fdiv(6.0,2.0)={:ok,{:finite,3.0}}`; NON-finite `fdiv(1.0,0.0)` → `{:ok,{:nonfinite,<<127,240,0,0,0,0,0,0>>}}` (+Inf, oracle raw bits `9218868437227405312`); f32 single-round `f32id(0.1)` re-encodes to `<<61,204,204,205>>` (0x3DCCCCCD); zero-result `noop()={:ok,:ok}`; threaded state `bump 5 then 10 = 15`, old instance immutable (`= 10`); Stateless `add(100,-1)={:ok,99}`. **Skip path proven non-vacuous:** temporarily probing a bogus exe name made the test print `[p12-04] elixirc not on PATH — Elixir compile+call SKIPPED` and the suite stayed green (1960 passed) — this is exactly what runs on CI (which does NOT install Elixir). The pure unit tests (determinism, single-file, zero-Elixir-dep, Threaded/Stateless `@spec` shapes) run ALWAYS.
  - **R23 — zero `Elixir.*` runtime deps (VERIFIED on a bare node).** The generated binding runs on a plain Erlang/OTP node with NO Elixir stdlib on the path: traps caught Erlang-style `try … catch :error, {:wasm_trap, _} = reason -> {:error, reason}` (NOT `rescue`, which pulls `Elixir.Exception.normalize/3` and `undef`s); integer masking is `:erlang.band/2`; signed decode is a GUARD clause (`defp i32_of_raw(r) when r >= 0x80000000 …`); the float codec is BEAM bit-syntax (`<<f::float-size(64)>>`); the instance is the raw `InstanceState` record threaded as a pure value with `@opaque instance :: tuple()` (**no `defstruct`** — a struct would drag `Enum` into `__struct__/1`). Empirically loaded+called an emitted `.ex` under `erl -pa ebin` (only the binding's ebin, no Elixir stdlib) and got the correct signed/float/nonfinite/trap values.
  - **Two-shape API rendered (R19).** `Stateless` (`module_b`): NO `instance`/`instantiate` — `def add(a0, a1)` re-instantiating internally (`st = :"twocore@wasm@xpure".instantiate()` inside the trap-catch, R5). `Threaded` (`module_a`): `instantiate() :: {:ok, instance()} | {:error, trap()}` (R5), a `touches_state` export threads back (`@spec bump(instance(), integer()) :: {:ok, {integer(), instance()}} | {:error, trap()}`; a 0-result mutator would collapse to `{:ok, instance()}`), a pure one takes `inst` and drops `st'` (`@spec add(instance(), integer(), integer()) :: {:ok, integer()} | {:error, trap()}`; 0-result pure `noop` → `{:ok, :ok}`).
  - **Float tagged tuple + bidirectional bit helpers (R18).** `@type floatval :: {:finite, float()} | {:nonfinite, binary()}`; guarded `f32_of_bits`/`f64_of_bits` classify on the all-ones exponent (f32 `0xFF` / f64 `0x7FF`) so NaN/±Inf never crash, and the decode lives in the `try … else` clause (Elixir runs `else` OUTSIDE the catch, like Erlang's `of`), so a valid non-finite result is never a fake trap (R7). Arg encodes bound with `=` BEFORE the `try`; only the `.beam` call is trap-protected. Helpers emitted only-when-referenced in the fixed order `i32_to_raw, i32_of_raw, i64_to_raw, i64_of_raw, f32_bits, f32_of_bits, f64_bits, f64_of_bits` (an unused `defp` is an `elixirc` warning) — the helper NAMES are drawn from `iface.reserved_names()` so no export `host_name` can collide with one. Verified warning-free under standalone `elixirc` (the only residual warnings are the unavoidable "target module not loaded", which vanish once the wasm `.beam` is on the path).
  - **⚠ Signals / spec deviations for the capstone (P12-06):**
    1. **No struct, no `rescue` — deliberate deviation from unit-spec §3's sample.** `04-emit-elixir.md` §3 sketches a `defstruct`/`%__MODULE__{}` instance and `catch :error, reason` (bare). R23 (authoritative) + running on a bare node forbid both: the struct risks an `Enum`/`__struct__` dep and the bare catch masks an arity/`undef` bug as a fake trap and pulls `Exception`. Implemented per R23/R7 (bare opaque `tuple()` + structural `{:wasm_trap, _}` catch), matching the Erlang sibling's shape.
    2. **Filename is `<base>_bindings.ex`, NOT `<module_name>.ex`.** The §3 sample's `<module_name>.ex` predates R14 (`module_name` = `twocore@wasm@<base>` with `@`); I use the legalized `<base>_bindings.ex` (sibling-consistent, no `@` in the filename). The Elixir module alias is `Camelize(base)Bindings`. The capstone reads `GeneratedFile.path` from the emitter, so this is self-consistent.
    3. **Dispatch call quoting is minimal for quality.** `:"twocore@wasm@x".add(inst,…)` (unquoted clean call) vs `:"twocore@wasm@x"."run-test"(…)` (quoted arbitrary name) — the module atom is always quoted (`@` requires it), the function name is quoted only when not a bare `[a-z_][A-Za-z0-9_]*` token (avoids the `elixirc` "quotes not required" warning; keywords like `when` parse fine unquoted after a `.`, verified).
  - **Files:** created `src/twocore/backend/emit_elixir_bindings.gleam`, `test/twocore/backend/emit_elixir_bindings_test.gleam`. No default-path source touched (drives only frozen public APIs + the shared test FFIs `twocore_bindings_ffi`/`twocore_threaded_test_ffi`).
- **P12-03 — Erlang emitter** — 1954 tests (was 1949, +5 `emit_erlang_bindings_test`), conformance unchanged (46,529/1,768/0), **0 failures**, 0 new warnings (the 2 pre-existing `rt_js_test` remain), `gleam format --check src test` clean, **default emission byte-identical** (a new emitter module + tests; the pipeline is untouched).
  - **`emit_erlang(Iface) -> List(GeneratedFile)`** (`src/twocore/backend/emit_erlang_bindings.gleam`) returns exactly **ONE** file — `<base>_bindings.erl` → `-module(<base>_bindings)` — because Erlang catches a BEAM exception IN-LANGUAGE (`try … catch error:{wasm_trap, _} -> …`), so NO companion catch shim (unlike Gleam's `.erl` shim). `<base> = iface.sanitize_identifier(module_name)` (`twocore@wasm@x` → `twocore_wasm_x`), matching the Gleam sibling's naming EXACTLY (uniform, NOT language-tagged — R16 collision noted below).
  - **Compile+call PROVEN (not golden).** The headline test builds two real modules (`describe` Threaded/Paged), `emit_erlang`s them, compiles the emitted `.erl` with the **real in-VM Erlang compiler** (`twocore_bindings_ffi:compile_load_erlang` → `compile:file`, always available), and calls typed exports through the compiled binding via `call/3` — each matching the WASM-spec value AND the raw in-process `.beam` oracle (`twocore_threaded_test_ffi`). Matched: signed int `add(2,-3)={ok,-1}` (raw ABI `4294967295`), `add(0x7FFFFFFF,1)={ok,-2147483648}`; multi-value `divmod(17,5)={ok,{3,2}}`, signed `divmod(-7,2)={ok,{-3,-1}}`; trap `divmod(10,0)={error,{wasm_trap,int_div_by_zero}}` (kind atom matched STRUCTURALLY); finite float `fdiv(6.0,2.0)={ok,{finite,3.0}}`; NON-finite `fdiv(1.0,0.0)` → `{ok,{nonfinite,<<127,240,0,0,0,0,0,0>>}}` (+Inf, oracle raw bits `9218868437227405312`); f32 single-round `f32id(0.1)` re-encodes to `<<61,204,204,205>>` (0x3DCCCCCD); zero-result `noop()={ok,ok}`; threaded state `bump 5 then 10 = 15`, old instance immutable (`= 10`); Stateless `add(100,-1)={ok,99}`. Anti-vacuity: the emitted `.erl` also verified to compile warning-free under standalone `erlc` (incl. `+warn_missing_spec`).
  - **Two-shape API rendered (R19).** `Stateless` (wholly pure): NO `instance()`/`instantiate/0` — `add(A0, A1) -> {ok, integer()} | {error, trap()}`, re-instantiating internally (`St = 'twocore@wasm@epure':instantiate(),` inside the trap-catch, R5). `Threaded` (has a state-reaching export): `instantiate() -> {ok, instance()} | {error, trap()}` (R5, NOT the unit-spec §3's bare `instance()` — R5 supersedes it), a `touches_state` export threads `instance()` back (`bump(instance(), integer()) -> {ok, {integer(), instance()}} | {error, trap()}`; a 0-result mutator collapses to `{ok, instance()}`), a pure one takes `Inst` and drops `St'` (`add(instance(), integer(), integer()) -> {ok, integer()} | {error, trap()}`).
  - **Float tagged tuple + bidirectional bit helpers (R18).** Float args/results are `-type floatval() :: {finite, float()} | {nonfinite, binary()}`; the guarded `dec_f32`/`dec_f64` classify on the all-ones exponent (f32 `16#FF` / f64 `16#7FF`) so NaN/±Inf never crash (`<<F:64/float>> = <<Bits:64>>` raises on non-finite), and the decode lives in the `try … of` clause — Erlang runs `of` OUTSIDE the catch, so a valid non-finite result is never a fake trap (R7). `enc_f*`/`dec_f*` ARE the bidirectional accessors (native↔raw bits), big-endian (canonical IEEE). Arg encodes bound with `=` BEFORE the `try`; only the `.beam` call is trap-protected. Helpers emitted only-when-referenced in the fixed order `enc_i32,enc_i64,enc_f32,enc_f64,dec_i32,dec_i64,dec_f32,dec_f64` (an unused private fn is an `erlc` warning). Types are `-export_type`'d (so none flags unused); `-moduledoc`/`-doc` (edoc) verified warning-free on OTP 29. Host names unquoted (guaranteed keyword-free by `iface`); module + dispatch atoms single-quoted (the `@`-bearing module atom and the arbitrary WASM export name, e.g. `'run-test'`).
  - **⚠ Signals for the Elixir emitter (P12-04) + capstone (P12-06):**
    1. **R16 collision is REAL and unresolved here (as designed).** The Erlang binding module `twocore_wasm_<x>_bindings` is the SAME BEAM atom as the Gleam binding's compiled module. The capstone MUST `code:purge`/`code:delete` between per-language runs (or the second load silently clobbers the first → false green). My test sidesteps this by using DISTINCT module bases (`estate`/`epure`) from the Gleam test's (`statemod`/`puremod`) so the shared test VM has no clash.
    2. **R5 supersedes unit-spec §3.** `03-emit-erlang.md` §3's sample shows `instantiate() -> instance().` (bare); RECONCILIATION R5 (authoritative) mandates the `Result` tuple `{ok, instance()} | {error, trap()}`, which is what I emit (matching the Gleam sibling). The unit-spec §3 arity sample (`add/3`) also assumed Threaded; under Stateless the host arity is `n` (no leading `Inst`) though the `.beam` is still `n+1` (re-instantiated internally).
    3. **`base = sanitize_identifier(module_name)`, NOT the "last @-segment"** the unit-spec §3.1 text suggests — I followed the Gleam sibling / the P12-03 prompt for cross-language uniformity (so both languages' bindings share the `twocore_wasm_<x>_bindings` base). The capstone's purge logic keys on this exact base.
  - **Files:** created `src/twocore/backend/emit_erlang_bindings.gleam`, `test/twocore/backend/emit_erlang_bindings_test.gleam`. No default-path source touched (drives only frozen public APIs + the shared test FFIs `twocore_bindings_ffi`/`twocore_threaded_test_ffi`).
- **P12-02 — Gleam emitter — the HEADLINE** — 1949 tests (was 1943, +6 `emit_gleam_bindings_test`), conformance unchanged (46,529/1,768/0), **0 failures**, 0 new warnings (the 2 pre-existing `rt_js_test` remain), `gleam format --check src test` clean, **default emission byte-identical** (a new emitter module + tests; the pipeline is untouched).
  - **`emit_gleam(Iface) -> List(GeneratedFile)`** (`src/twocore/backend/emit_gleam_bindings.gleam`) returns **THREE** files (basenames, for the CLI to write under the consuming `src/`, R22): `<base>_bindings.gleam` (the typed API), `<base>_bindings_ffi.erl` (the catch-shim), `<base>_bindings_README.md` (usage). `<base>` = `iface.sanitize_identifier(module_name)` (`twocore@wasm@x` → `twocore_wasm_x`) so the binding module atom can NEVER clobber the loaded `.beam` atom.
  - **Compile+call PROVEN (not golden).** The headline test builds two real modules (`describe` Threaded/Paged), `emit_gleam`s them, drops both bindings + shims + a hand-written driver into a temp Gleam project, `gleam build`s it with the **real toolchain** (`twocore_bindings_ffi:compile_load_gleam`), and calls typed exports through the compiled binding — each matching the WASM-spec value AND the raw in-process `.beam` oracle (`twocore_threaded_test_ffi`). Matched: signed int `add(2,-3)=Ok(-1)` (raw ABI `4294967295`), `add(0x7FFFFFFF,1)=Ok(-2147483648)`; multi-value `divmod(17,5)=Ok(#(3,2))`, signed `divmod(-7,2)=Ok(#(-3,-1))`; trap `divmod(10,0)=Error(Trap("{wasm_trap,int_div_by_zero}"))`; finite float `fdiv(6.0,2.0)=Ok(Finite(3.0))`; NON-finite `fdiv(1.0,0.0)` → `NonFinite(<<127,240,0,0,0,0,0,0>>)` (+Inf, raw bits `9218868437227405312`); f32 single-round `f32id(0.1)` → `<<61,204,204,205>>` (0x3DCCCCCD); zero-result `noop()=Ok(Nil)`; threaded state `bump 5 then 10 = 15`, old instance immutable (`= 10`); Stateless `b.add(100,-1)=Ok(99)`. Anti-vacuity confirmed (breaking an expected value reddens the suite — the compiled binding genuinely runs).
  - **Two-shape API rendered (R19).** `Stateless` (`module_b`, wholly pure): NO `Instance`/`instantiate` — `pub fn add(a0: Int, a1: Int) -> Result(Int, Trap)`, re-instantiating internally (`let st = raw_instantiate()` inside the `rescue` thunk). `Threaded` (`module_a`, has a state-reaching `bump`): `pub fn instantiate() -> Result(Instance, Trap)` (R5 — a trapping start/OOB segment is caught), a `touches_state` export `pub fn bump(inst: Instance, a0: Int) -> Result(#(Int, Instance), Trap)`, a pure one `pub fn add(inst: Instance, a0: Int, a1: Int) -> Result(Int, Trap)` (takes `inst`, drops `St'`).
  - **Float sum type + bidirectional raw-bits accessors (R18).** Emits `pub type FloatVal { Finite(Float) NonFinite(BitArray) }` guarded on the all-ones exponent (f64 `0x7FF`/f32 `0xFF`), with `pub fn f64_bits`/`f64_of_bits`/`f32_bits`/`f32_of_bits` (widths from `value_abi`, not `host_types`). Native `Float` only on the guaranteed-finite path. **R7 honored:** arg conversions bound with `let` BEFORE `rescue`, result conversions in the `Ok` arm AFTER — only the `.beam` call is inside the shim's try/catch. The shim matches `error:{wasm_trap, _}` STRUCTURALLY (never a bare catch-all).
  - **⚠ DISCOVERED-WRONG / signals for the siblings + capstone:**
    1. **Prelude-only is load-bearing** (already baked into the frozen FFI header, but the unit spec §3 sketches `import gleam/int`/`gleam/result`/`gleam/dynamic` — those would NOT build in the dependency-free harness). The emitted `.gleam` uses ONLY the prelude + `@external`: the opaque runtime term is an `@external` type (`type RawState`, `type RawTerm` — external types with no constructors), signed-int wrap is `%` + a branch (not `int.bitwise_and`), `Result` is `case`-destructured (not `result.map`), float codec is bit-array syntax (`<<f:float-size(64)>>` / `<<_s:1, exp:11, _m:52>>`). The Erlang/Elixir siblings have their host stdlib and are NOT so constrained.
    2. **THREE files, not "exactly two"** — the unit spec §3/§7 say "exactly two `GeneratedFile`s", but R22 (authoritative) mandates a usage README, so `emit_gleam` returns three. **P12-05 (CLI) must write all three** into `--out` and not assume two.
    3. **R16 cross-language atom clash is NOT resolved here.** Per the unit spec §3.1/§7 the binding module is `<base>_bindings` (NOT language-tagged), so an Erlang `<base>_bindings` and this Gleam `<base>_bindings` are the SAME BEAM atom — **the capstone (P12-06) MUST `code:purge`/`code:delete` between per-language runs** (or language-tag), else a false green (R16's other option).
    4. **`describe` requires the pipeline to lower under `profiles.portable()`** (Threaded+Paged+TablePaged) — the emitter emits the `.beam` and `describe`s from the SAME binding, so the arity/`touches_state` agree (R1/R17); P12-05 must hand ONE lowered module to both.
  - **Files:** created `src/twocore/backend/emit_gleam_bindings.gleam`, `test/twocore/backend/emit_gleam_bindings_test.gleam`. No default-path source touched (drives only frozen public APIs + the shared test FFIs).
- **P12-01 — KEYSTONE — `«IFACE-DESC-FROZEN» ✓`** — 1943 tests (was 1922, +21 `iface_freeze_test`), conformance unchanged (46,529/1,768/0), **0 failures**, 0 new warnings (the 2 pre-existing `rt_js_test` remain), `gleam format --check src test` clean, **default emission byte-identical** (the only `src/` edit outside the new leaf module is promoting one private fn to `pub` — same callers, same output; the `.beam`/`.core` pipeline is untouched).
  - **FROZEN shapes** (`src/twocore/backend/iface.gleam` — P12-02/03/04/05/06 build against these verbatim):
    - `pub type Iface { Iface(module_name: String, state_model: StateModel, exports: List(ExportSig)) }` — `module_name` is `module.name` VERBATIM (`twocore@wasm@<base>`, R8/R14 — NOT normalized).
    - `pub type StateModel { Stateless  Threaded }` (R3/R19) — `Stateless` = whole-module state-reaching closure empty (no `Instance`/`instantiate` in the host surface); `Threaded` = ≥1 export threads state. Governs ONLY the host surface, not the `.beam` arity.
    - `pub type ExportSig { ExportSig(host_name: String, dispatch_atom: String, params: List(ir.ValType), results: List(ir.ValType), touches_state: Bool, emitted_arity: Int, leading_state: Bool) }` — `dispatch_atom` = exact `ExportFn.export_name` (verbatim in `@external`/`apply`, R9); `host_name` = deterministic language-legal identifier (Gleam `[a-z][a-z0-9_]*`, non-keyword, non-reserved — hence legal unquoted in all 3 langs; collision-resolved with `_2`/`_3`, R9/R15); `touches_state` = the TRANSITIVE state-reaching closure (R1); `emitted_arity` = `len(params)+1` (R2/R19 — mirrors `emit_core` EXACTLY, freeze-tested equal to the real `.core`); `leading_state = True` for every accepted (Threaded) export.
    - `pub type GeneratedFile { GeneratedFile(path: String, content: String) }`.
    - `pub type IfaceError { CellUnsupported  ImportBearingUnsupported  MutableTierUnsupported }` (R13 + R20). Unresolved `ExportFn.fn_name` post-validation is a documented `let assert`, NOT a variant.
    - `pub fn describe(module: ir.Module, binding: instance.Binding) -> Result(Iface, IfaceError)` — on the LOWERED+optimized module (R1/R17); fail-closed order: `Cell`→`CellUnsupported`, `imports != []`→`ImportBearingUnsupported`, tier ∉ {`Paged`+`TablePaged`}→`MutableTierUnsupported`; ExportFn-only (skips `ExportGlobal`/`Table`/`Memory`/`Tag`, R13); `state_model = Threaded` iff any export `touches_state`.
    - **Uniform emitter signature:** `pub type Emitter = fn(Iface) -> List(GeneratedFile)` — the frozen contract all three `emit_gleam`/`emit_erlang`/`emit_elixir` + the CLI build against (§3.5; a `List` because Gleam needs a companion `.erl` catch-shim `GeneratedFile`).
    - **Value-ABI as DATA** (emitters cannot drift): `pub fn host_types(ir.ValType) -> HostTypeNames(gleam, erlang, elixir)` (P2 table: i32/i64→`Int`/`integer()`/`integer()`, f32/f64→`Float`/`float()`/`float()`, v128→`BitArray`/`binary()`/`binary()`, ref/term→`Ref`/`term()`/`term()`); `pub fn value_abi(ir.ValType) -> ValueAbi { IntAbi(width) FloatAbi(width) V128Abi RefAbi }` (the float codec width — TF32⇒32, TF64⇒64 — comes from HERE, never `host_types`, R18); `pub fn result_encoding(List(ir.ValType)) -> ResultEncoding { ResultUnit ResultBare ResultTuple(arity) }` (R4: 0→`ok`, 1→bare, N≥2→N-tuple). `pub fn sanitize_identifier(String) -> String` (the single-name rule, `pub` for reuse). All ValType `case`s exhaustive (no `_`).
  - **ABI note the emitters MUST honor (R2/R19, grounded in `emit_core.emit_fn_export`):** under the (always-`Threaded`) accepted binding, `emit_core` emits EVERY export UNIFORMLY at arity `n+1` — a state-reaching export directly (`fun(St, A…) -> {ResultPackage, St'}`), a PURE export via a thin adapter (`fun(St, A…) -> {apply 'g'/n(A…), St}`) — both `n+1`, both leading-`St`, both returning the `{ResultPackage, St'}` 2-tuple. `touches_state` governs ONLY whether the HOST surface threads the `Instance` back. Confirmed in the emitted `.core` by `export_sig_arity_matches_emitted_core_test` (pure `add`⇒arity 3, stateful `writer`⇒arity 1, each `== emit_core.emit_module`'s `FName` arity).
  - **Cross-file reach (P12-01-owned, recorded here):** promoted `emit_core.state_reaching_closure/1` from `fn` to **`pub`** (unchanged body/callers — the transitive `CallDirect` fixpoint over the 7 stateful-node seeds) so `describe/2` derives `touches_state` from the IDENTICAL source of truth `emit_core` uses to set each export's emitted arity (R1). Changes no emitted output. The shallow `expr_touches_state` stays private (it is NOT the descriptor's input — the `transitive_state_reaching_test` proves a pure-bodied export calling a `MemStore` helper is `touches_state == True`).
  - **Shared compile+call FFI companion (R25b), pulled forward for the Wave-A emitter DoDs** — `test/twocore_bindings_ffi.erl` (test-only trust boundary: `os`/`file`/`compile`/`beam_lib`/`code`/`open_port`). Frozen entries P12-02/03/04/06 consume: `which(Exe::binary) -> {ok, Path}|{error, nil}` (toolchain-presence gate, for Elixir best-effort); `compile_load_erlang(Files, Main) -> {ok, Main}|{error, Text}` (in-VM `compile:file`, serves the Erlang emitter + Gleam's `.erl` shim); `compile_load_gleam(Files, Main) -> {ok, Main}|{error, Text}` (stages a dependency-free temp Gleam project, `gleam build --target erlang`, loads the ebin — the bindings use only prelude + `@external`, so it builds OFFLINE); `compile_load_elixir(Files, Main) -> {ok, Main}|{error, Text}` (`elixirc`, best-effort/skip-if-absent); `call(M, F, Args) -> {ok, Value}|{error, Text}` (apply with exception→`{error, Text}` safety net). `Files :: [{FileName::binary, Content::binary}]` (basename WITH extension); "compile ONCE per (language × module), call many exports." All three compile paths are DRIVEN + proven in `iface_freeze_test` (Erlang value+trap-raise fixtures in-VM; a real `gleam build` + call; an `elixirc` + call gated on `which("elixirc")`).
  - **Discovered/confirmed for downstream:** R19's "uniformly `n+1` under Threaded" is CONFIRMED against the real codegen (not just asserted). Spec §3.1 named `StateModel { ImportFree Threaded }` — SUPERSEDED by R3 to `{ Stateless Threaded }` (implemented). `host_types` returns the naive scalar `Float` per the §3.4 table (freeze-tested); the non-finite `Finite(Float)|NonFinite(BitArray)` SUM-TYPE wrapper is emitter-owned and takes its raw-bits codec width from `value_abi/1` (R18), so the keystone carries both. `host_name` is a SINGLE cross-language-legal identifier (strictest = Gleam grammar + the UNION of Gleam/Erlang/Elixir keywords + generated API names reserved), so one name is valid in all three emitters — the reserved set intentionally over-approximates (a needless collision only costs a `_2`).
  - **Files:** created `src/twocore/backend/iface.gleam`, `test/twocore/backend/iface_freeze_test.gleam`, `test/twocore_bindings_ffi.erl`; edited `src/twocore/backend/emit_core.gleam` (the one `pub` promotion + its doc). No default-path behavior changed.

---

## Template (copy this block when the next phase starts)

> Phase N — «title». Goal & honest scope: see the phase overview. Decisions: `Q1–Q8` (continue the
> letter series — Phase 12 used `P`; `Q1` = keystone, `Q8` = honest scope). All prior-phase decisions
> and the invariants in [`03-phase-workflow.md`](03-phase-workflow.md) §8 still hold.

### Freeze milestones

| Milestone | Produced by | Status | Unblocks |
|---|---|---|---|
| `«X-FROZEN»` — … | 01 | `unclaimed` | … |

### Units

| Unit | Owner / status | Depends on (freeze) | Leaves |
|---|---|---|---|
| **01** Interface freeze (keystone) | `unclaimed` | — | … |
| **…** | `unclaimed` | … | … |
| **N** Capstone | `unclaimed` | all above | **PHASE N PROVEN.** … |

### Landing log

_(one line per landing: `unit — N tests (was M, +K), conformance p/s/f, 0 warnings, format clean, byte-identical`)_
